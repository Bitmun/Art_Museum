import { HomeDisplay } from 'components/HomeDisplay';
import { MainContainer } from 'styles';

export const Home = () => {
  return (
    <MainContainer>
      <HomeDisplay />
    </MainContainer>
  );
};
